<ResourceDictionary
    x:Class="NINA.Plugin.SeeDrift.Resources"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:NINA.Plugin.SeeDrift"
    xmlns:vm="clr-namespace:NINA.Plugin.SeeDrift.ViewModels"
    xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf">

    <GeometryGroup x:Key="SeeDrift_Icon" FillRule="Nonzero">
        <PathGeometry Figures="M3,12 L21,12 M12,3 L12,21 M6,6 L18,18 M18,6 L6,18"/>
    </GeometryGroup>

    <DataTemplate x:Key="NINA.Plugin.SeeDrift.ViewModels.DriftPlotDockableVM_Dockable" DataType="{x:Type vm:DriftPlotDockableVM}">
        <Grid Margin="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,6">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="Reset" Command="{Binding ResetCommand}" Margin="0,0,8,0" Padding="8,4"/>
                    <Button Content="Import FITS folder…" Command="{Binding ImportFolderCommand}" Margin="0,0,8,0" Padding="8,4"/>
                    <Button Content="Save report now" Command="{Binding SaveReportNowCommand}" Padding="8,4"/>
                    <TextBlock Text="{Binding DriftModeLabel, StringFormat=' · {0}'}"
                               VerticalAlignment="Center" Foreground="#888" Margin="8,0,0,0" FontSize="11"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Margin="0,0,5,0" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#555"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsArmed}" Value="True">
                                        <Setter Property="Fill" Value="#4caf50"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock VerticalAlignment="Center" Foreground="#AAA">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Not recording"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsArmed}" Value="True">
                                        <Setter Property="Text" Value="Recording"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding SampleCount, StringFormat=' · {0} frames'}"
                               VerticalAlignment="Center" Foreground="#AAA" Margin="2,0,0,0"/>
                </StackPanel>
            </DockPanel>
            <TextBlock Grid.Row="1" Text="{Binding LastSummary}" TextWrapping="Wrap" Foreground="#CCC" Margin="0,0,0,8"/>
            <oxy:PlotView Grid.Row="2" Model="{Binding PlotModel}" Controller="{Binding PlotController}" MinHeight="240"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="SeeDrift_Options" DataType="{x:Type local:SeeDriftPlugin}">
        <StackPanel Margin="8" MaxWidth="560">

            <TextBlock Text="Drift source" FontWeight="Bold" Margin="0,0,0,6"/>
            <StackPanel Margin="0,0,0,4">
                <RadioButton Content="FITS header coordinates (fast, uses RA/Dec from headers)"
                             GroupName="DriftSourceMode"
                             IsChecked="{Binding FolderImportUsesHeaderCoordinates}"
                             Margin="0,0,0,4"/>
                <RadioButton Content="Pixel registration (slower, reads image data — more accurate for Seestar)"
                             GroupName="DriftSourceMode"
                             IsChecked="{Binding FolderImportUsesPixelRegistration}"
                             Margin="0,0,0,4"/>
            </StackPanel>
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Registration crop (px):" VerticalAlignment="Center" Margin="0,0,8,0"
                           IsEnabled="{Binding FolderImportUsesPixelRegistration}"/>
                <TextBox Grid.Column="1"
                         Text="{Binding RegistrationCropSize, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding FolderImportUsesPixelRegistration}"
                         VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="  px (centre crop compared frame-to-frame)"
                           VerticalAlignment="Center" Foreground="#888" FontSize="11"
                           IsEnabled="{Binding FolderImportUsesPixelRegistration}"/>
            </Grid>

            <TextBlock Text="HTML export" FontWeight="Bold" Margin="0,0,0,6"/>
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Default folder:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" Text="{Binding HtmlExportFolder, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>
